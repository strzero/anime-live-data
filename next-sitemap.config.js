/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://live.s0tools.cn', // 你的站点网址，必须写正确
  generateRobotsTxt: true, // 自动生成robots.txt
  // 你还可以添加额外的配置，比如排除某些路径
  // exclude: ['/secret-page', '/admin/*'],
};
