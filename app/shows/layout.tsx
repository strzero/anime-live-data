export const metadata = {
  title: '演出列表 - Anime Live DB',
  description: '按条件筛选演出许可数据，支持多维度查询。',
};
import { ReactNode } from 'react';

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>; 
}